var div = document.querySelector('#search-results')

<% elements = @search.map { |search| j render partial: @class_name.downcase, locals: { s: search }} %>

div.innerHTML = "<%= elements.join('').html_safe %>"
