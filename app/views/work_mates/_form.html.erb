<%= form_with(model: work_mate, local: true) do |form| %>
  <% if work_mate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_mate.errors.count, "error") %> prohibited this work_mate from being saved:</h2>

      <ul>
      <% work_mate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :work_id %>
    <%= form.text_field :work_id %>
  </div>

  <div class="field">
    <%= form.label :producer_id %>
    <%= form.text_field :producer_id %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.number_field :state %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
