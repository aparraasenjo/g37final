<p id="notice"><%= notice %></p>

<h1>Work Mates</h1>

<table>
  <thead>
    <tr>
      <th>Work</th>
      <th>Producer</th>
      <th>State</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @requested.each do |r| %>
      <tr>
        <td><%= r.work %></td>
        <td><%= r.producer %></td>
        <td><%= r.state %></td>
        <td><%= link_to 'Show', r %></td>
        <td><%= link_to 'Edit', edit_work_mate_path(r) %></td>
        <td><%= link_to 'Destroy', r, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


